<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.parse.starter">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".StarterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="InstaMiliGram"
        android:theme="@style/AppTheme">
        <activity android:name=".chatActivity"></activity>
        <activity android:name=".messageUserListActivity" />
        <activity android:name=".MFabButtons" />
        <activity android:name=".postUploadActivity" />
        <activity android:name=".mainUserFeedActivity" />
        <activity android:name=".singleUserFeedActivity" />
        <activity android:name=".userListActivity" />
        <activity
            android:name=".loginActivity"
            android:label="InstaMiliGram">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>